{
  "name": "@buocvang/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript SDK for the Bước Vàng Public API",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "generate": "echo 'SDK is hand-maintained from services/api/openapi.yaml for now'",
    "lint": "tsc --noEmit",
    "test": "echo 'no tests yet'"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
